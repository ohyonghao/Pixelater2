#ifndef JARVISMARCH_H
#define JARVISMARCH_H
#include <vector>
#include "point.h"

std::vector<point> jarvisMarch(std::vector<point>&);
std::vector<point> grahamScan(std::vector<point>&);
#endif // JARVISMARCH_H
